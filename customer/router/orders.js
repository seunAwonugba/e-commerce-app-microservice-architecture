const express = require("express");
// const { createOrder } = require("../controller/orders");
const ordersRouter = express.Router();

// ordersRouter.post("/create-order", createOrder);

module.exports = { ordersRouter };
